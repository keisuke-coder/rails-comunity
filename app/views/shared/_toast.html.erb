
<% if flash[:notice].present? %>
  <div class="toast">
    <div class="notice"><%= notice %></div>
  </div>
<% end %>

<% if flash[:alert].present? %>
  <div class="toast">
    <p class="alert"><%= alert %></p>
  </div>
<% end %>